# Kept in sync with vite.config.ts's build.target - this app ships inside an Android
# WebView on TV hardware that's frequently years old with no auto-updating browser
# engine. autoprefixer (see postcss.config.js) uses this to decide which vendor
# prefixes this app's CSS still needs (backdrop-filter, etc.).
chrome >= 65
firefox >= 60
safari >= 11
edge >= 79
