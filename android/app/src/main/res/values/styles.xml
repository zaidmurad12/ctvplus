<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Without this, AppCompat's DayNight theme paints the window background white for
             the brief window between process start and the first React frame - the "white
             screen at boot" complaint. Matching the app's own dark background here means
             that gap is invisible instead of a flash of white. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

</resources>
