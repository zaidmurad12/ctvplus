<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lets the app itself prompt to install an APK it downloaded (ApkUpdaterModule) - the
         in-app update check, without which the system silently blocks the install intent
         regardless of any "install unknown apps" toggle (that toggle doesn't even appear for
         this app in Settings without this permission declared). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:banner="@drawable/tv_banner"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true"
      android:largeHeap="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:banner="@drawable/tv_banner"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:screenOrientation="sensorLandscape"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Hands the APK ApkUpdaterModule downloaded itself to the system installer via a real
           content:// URI (a raw file:// URI across the app boundary throws
           FileUriExposedException on modern Android). Replaced Android's own DownloadManager,
           which turned out to just never move past 0% on several real devices - the system
           Download Manager component many Android TV/set-top boxes ship with a stripped-down or
           entirely disabled Downloads app, so enqueue() would accept the request without
           anything ever actually running it. -->
      <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
      </provider>
    </application>
</manifest>
